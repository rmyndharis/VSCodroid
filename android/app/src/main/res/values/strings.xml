<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="app_name">VSCodroid</string>
    <string name="notification_channel_name">VSCodroid Server</string>
    <string name="notification_channel_description">Keeps the local development server running</string>
    <string name="notification_title">VSCodroid is running</string>
    <string name="notification_text">Local development server active</string>
    <!-- Replaces the line above when the server was adopted rather than started.
         An adopted server outlived the bootstrap that forked it, and the DNS
         proxy died with that bootstrap while the survivor kept the dead address
         in its environment. Everything honouring HTTPS_PROXY then fails for the
         rest of the session: the extension marketplace, the agent host, and git,
         npm and curl in every terminal, which inherit it. The editor looks
         healthy throughout, so this card is the only thing a user can see. -->
    <string name="notification_text_adopted">No network for extensions, git or npm. Restart the app to fix it.</string>
    <string name="action_stop">Stop Server</string>

    <!-- Shown once the automatic restart budget is spent. Replaces the two
         strings above in the same notification, which then carries no Stop
         action because there is nothing left to stop. -->
    <string name="notification_title_stopped">VSCodroid server stopped</string>
    <string name="notification_text_stopped">Server crashed repeatedly. Please restart the app.</string>
    <string name="extracting_message">Extracting core files\u2026</string>
    <string name="error_server_start">Failed to start development server</string>
    <string name="error_server_timeout">Server took too long to start</string>

    <!-- Deliberately not an error_ string, and deliberately not the wording of
         the one above it. This is said while the server is still being waited
         for and may still come up, so it must not read as an ending: the app
         goes on asking after saying it, and the editor opens if an answer
         arrives. Borrowing the terminal wording would make a slow start
         indistinguishable from a dead one to everyone downstream.

         It also has to survive being read later. The next activity to bind
         reads it through NodeService.lastStartupNotice() with no idea how long
         ago it was recorded, so it says nothing about elapsed time. -->
    <string name="status_server_slow_start">The development server is taking longer than usual to start. The editor will open as soon as it is ready.</string>
    <string name="status_updating_python">Updating Python…</string>
    <!-- The figure is passed in rather than written here: it is the size of the
         bundled asset tree, which grows with every VS Code pin. A fixed 500 MB
         outlived the tree it was measured against and sent people to free half
         of what unpacking actually needs, so they cleared space, came back, and
         failed in the same place. See FirstRunSetup.requiredStorageMb(). -->
    <string name="error_storage_full">Not enough storage space. Free at least %1$d MB and try again.</string>
    <string name="error_setup_failed">Setup failed. Tap Retry or reinstall the app.</string>
    <!-- The same failure when setup knows which step it died on. Names the step
         and quotes the exception, because the audience is developers and the
         next thing they do is either free some space or file the message. The
         version without a cause stays for a failure that happens before the
         first progress report. -->
    <string name="error_setup_failed_at">Setup failed while: %1$s\n\n%2$s\n\nTap Retry, or reinstall the app if it keeps failing.</string>

    <!-- A tap on a link this app handed to Android that no app took. Names the
         scheme rather than the whole address, which can be long and can carry a
         token, and says the one thing the user can act on: nothing here is
         broken, there is simply nothing installed that opens this kind of link. -->
    <string name="url_handoff_no_app">No installed app opens %1$s links.</string>
    <!-- The same tap failing for any other reason: a file:// URI Android refuses
         to expose, a content:// without a grant, anything else. The exception
         type is quoted because the audience is developers and it is the part
         worth putting in a bug report. -->
    <string name="url_handoff_failed">Could not open that %1$s link (%2$s).</string>

    <!-- Shown on the page itself, not in a toast, when the restart budget is
         spent. The screen behind it read "Starting server..." and went on
         reading that for ever, so the only thing it said was the only thing
         that was no longer true. -->
    <string name="error_server_gave_up">The development server stopped and could not be restarted. Your files are safe. Trying again usually works; if it does not, close and reopen the app.</string>
    <string name="error_server_retry">Try again</string>
    <string name="legal_disclaimer">VSCodroid is built from the MIT-licensed Code - OSS source code.\nNot affiliated with or endorsed by Microsoft Corporation.\n\"Visual Studio Code\" and \"VS Code\" are trademarks of Microsoft.\nUses Open VSX extension registry, not Microsoft Marketplace.</string>

    <!-- About dialog -->
    <string name="about_title">About VSCodroid</string>
    <string name="about_version_format">Version %s</string>
    <string name="about_licenses">Licenses</string>
    <string name="licenses_title">Open Source Licenses</string>
    <!-- Opens the verbatim GPL and LGPL texts. Separate from the notices above
         because the copyleft binaries in this app require the licence itself to
         be handed over, not only named. -->
    <string name="licenses_full_texts">License Texts</string>

    <!-- Toolchain picker (first-run) -->
    <string name="picker_title">What do you code in?</string>
    <string name="picker_subtitle">Optional \u2014 touch and hold the app icon to add or remove these later.</string>
    <string name="picker_included">Node.js, Python, and Git are already included.</string>
    <string name="picker_continue">Continue</string>
    <string name="picker_skip">Skip</string>

    <!-- Toolchain download progress -->
    <string name="progress_title">Installing toolchains\u2026</string>
    <string name="progress_waiting">Waiting\u2026</string>
    <string name="progress_installing">Installing\u2026</string>
    <string name="progress_done">Done</string>
    <string name="progress_failed">Failed</string>

    <!-- Why an install failed. "Failed" alone was the same word for a full disk,
         a dropped connection and a file the release never published, and the user
         is the one person who can act on the difference. Each of these names the
         thing they can do about it; ToolchainFailure decides which one is shown. -->
    <string name="toolchain_failed_network">Download failed. Check your connection and try again.</string>
    <string name="toolchain_failed_storage">Not enough storage. Free some space and try again.</string>
    <string name="toolchain_failed_not_published">This toolchain is not in the current release. Try again after the next app update.</string>
    <string name="toolchain_failed_digest">The download did not match what the release published. Try again.</string>
    <string name="toolchain_failed_corrupt">The download was incomplete. Try again.</string>
    <string name="toolchain_failed_play">Play delivers this toolchain only to apps it installed. Install from the Play Store and try again.</string>
    <string name="toolchain_failed_internal">Install failed. The app log has the details.</string>
    <string name="progress_cancel">Cancel</string>
    <string name="progress_retry">Retry</string>

    <!-- Toolchain settings -->
    <string name="toolchain_settings_title">Toolchains</string>
    <string name="toolchain_installed">Installed</string>
    <string name="toolchain_install">Install</string>
    <string name="toolchain_remove">Remove</string>
    <string name="toolchain_cancel">Cancel</string>
    <string name="toolchain_remove_confirm_title">Remove %s?</string>
    <string name="toolchain_remove_confirm_message">This will delete all %s files. You can reinstall anytime.</string>

    <!-- Launcher shortcut: in practice the only route to the toolchain screen.
         The activity is not exported and has no launcher entry, and the one
         command that could reach it through the WebView is sent by no bundled
         extension. SplashActivity.publishToolchainShortcut records both.

         The long label is the one that has to survive truncation, not the short
         one. The launcher's touch-and-hold menu shows longLabel: read off a
         device, that row renders as "Manage toolchai...", cut off mid-word. So
         any screen telling a user what to look for has to name the long label,
         which is what the Get Started walkthrough now does. -->
    <string name="shortcut_toolchains_short">Toolchains</string>
    <string name="shortcut_toolchains_long">Manage toolchains</string>
    <string name="toolchain_size_download_and_installed">%1$s download, %2$s installed</string>
    <!-- Shown when a save could not be written out to the device folder. Names the
         consequence rather than the error: the user's next decision is whether to
         copy the file out before uninstalling, not which exception was thrown. -->
    <string name="saf_write_back_failed">%1$s did not reach the device folder. The only copy is inside VSCodroid.</string>

    <!-- Shown once after a folder opens when documents in it could not be copied into
         the editor: a provider error, or no room left. The opposite of
         saf_write_back_failed on purpose. There the app holds the only copy; here the
         device does, and the file is simply absent from the editor. -->
    <plurals name="saf_documents_not_copied">
        <item quantity="one">A file could not be copied into the editor. It is unchanged in the device folder.</item>
        <item quantity="other">%1$d files could not be copied into the editor. They are unchanged in the device folder.</item>
    </plurals>
    <!-- The same shortfall when the sizes the provider reported did not fit in the space
         left. Named separately because it is the one the user can act on. -->
    <plurals name="saf_documents_not_copied_no_room">
        <item quantity="one">Not enough free space: a file could not be copied into the editor. It is unchanged in the device folder.</item>
        <item quantity="other">Not enough free space: %1$d files could not be copied into the editor. They are unchanged in the device folder.</item>
    </plurals>

    <!-- A folder copied out to the device that did not arrive whole. Counts entries
         rather than naming them, because the failure is usually folder-wide and forty
         separate notices is another way of telling the user nothing. Deliberately not
         worded like saf_write_back_failed: most of the folder did arrive, so "the only
         copy is inside VSCodroid" would overstate. -->
    <plurals name="saf_upload_incomplete">
        <item quantity="one">%2$s: one item did not reach the device folder.</item>
        <item quantity="other">%2$s: %1$d items did not reach the device folder.</item>
    </plurals>
    <!-- The same folder stopped by the enumeration cap rather than by a refusal. Named
         separately because the cause is a limit this app chose, not the device saying
         no, and the user's next move is to copy the rest out by hand. -->
    <string name="saf_upload_capped">%1$s is too large to copy out whole. The rest of it stayed inside VSCodroid.</string>

    <!-- The Toolchains toolbar's only icon-only control, and the app's only visible
         way back out of that screen. Without it a screen reader announces an
         unlabelled button: setSupportActionBar is never called, so the framework's
         own "Navigate up" default never applies. -->
    <string name="toolchain_navigate_up">Back to the editor</string>
</resources>
