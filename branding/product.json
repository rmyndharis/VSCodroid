{
  "$comment": [
    "Applied over the Code - OSS product.json before gulp runs, by",
    "scripts/build-vscode-oss.sh. Doing it at build time rather than patching the",
    "packaged tree afterwards means the values are baked in wherever the build",
    "inlines them, not just where a later regex happened to reach.",
    "",
    "assets/server.js rewrites product.json again at every start, and that is",
    "where extensionsGallery is set -- deliberately there and not here. At build",
    "time the gallery named in product.json is where the bundled js-debug",
    "extensions are downloaded from, and pointing that at Open VSX broke the",
    "build: it serves repackaged copies, and the checksums product.json pins no",
    "longer match them. Left unset, the download goes to each extension's own",
    "GitHub release, where they do.",
    "",
    "So do not add extensionsGallery to \"set\". It was there once and getting it",
    "back out cost three build attempts, because this overlay is applied with",
    "update() and a reused work volume kept serving the key that had already been",
    "removed from the file -- see the Branding stage in build-vscode-oss.sh.",
    "",
    "What is true of both: server.js merges shallowly, so a nested object it",
    "names replaces the built one wholesale. Nothing here may rely on a nested",
    "key surviving that merge.",
    "",
    "sharedDataFolderName renames a folder nothing currently creates, and it is",
    "set for that reason rather than in spite of it. Measured on the packaged",
    "tree: appSharedDataHome, the one reader of the key, appears only as a getter",
    "definition and a memoize registration in four bundles, with no caller",
    "anywhere, and the browser workbench builds its own from userRoamingDataHome",
    "without consulting the key at all. So no ~/.vscode-oss-shared exists on any",
    "install and there is nothing to migrate; what this buys is that an upstream",
    "version which starts calling that getter writes ~/.vscodroid-shared instead."
  ],
  "remove": [
    "webviewContentExternalBaseUrlTemplate",
    "darwinBundleIdentifier",
    "linuxIconName",
    "tunnelApplicationName",
    "nodejsRepository",
    "win32AppUserModelId",
    "win32DirName",
    "win32MutexName",
    "win32NameVersion",
    "win32RegValueName",
    "win32ShellNameShort",
    "win32TunnelMutex",
    "win32TunnelServiceMutex",
    "win32arm64AppId",
    "win32arm64UserAppId",
    "win32x64AppId",
    "win32x64UserAppId",
    "voiceWsUrl",
    "agentsTelemetryAppName",
    "darwinProfileUUID",
    "darwinProfilePayloadUUID",
    "electronArtifactFeed",
    "nodejsArtifactFeed"
  ],
  "set": {
    "nameShort": "VSCodroid",
    "nameLong": "VSCodroid",
    "applicationName": "vscodroid",
    "dataFolderName": ".vscodroid",
    "serverApplicationName": "vscodroid-server",
    "serverDataFolderName": ".vscodroid-server",
    "sharedDataFolderName": ".vscodroid-shared",
    "urlProtocol": "vscodroid",
    "quality": "stable",
    "linkProtectionTrustedDomains": [
      "https://open-vsx.org"
    ],
    "telemetryOptIn": false,
    "enableTelemetry": false,
    "reportIssueUrl": "https://github.com/rmyndharis/VSCodroid/issues/new",
    "licenseUrl": "https://github.com/rmyndharis/VSCodroid/blob/main/LICENSE",
    "serverLicenseUrl": "https://github.com/rmyndharis/VSCodroid/blob/main/LICENSE"
  }
}
